// Reveal.initialize();

  Reveal.initialize({
    controls: true,
    progress: true,
    // defaultTiming: 120,
    loop: true,
    slideNumber: true,
    // autoSlide: 9000,
    autoSlideStoppable: true,
    hideAddressBar: true,
    transition: 'convex',
    transitionSpeed: 'slow',
    overview: true,
  });